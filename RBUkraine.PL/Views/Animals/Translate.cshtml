@model RBUkraine.PL.ViewModels.Animals.AnimalTranslateEditorViewModel

@{
    ViewData["Title"] = "Translate";
}

<h1>Translate</h1>

<h4>AnimalTranslateEditorViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Translate">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Species" class="control-label"></label>
                <input asp-for="Species" class="form-control" />
                <span asp-validation-for="Species" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="GetAll">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
